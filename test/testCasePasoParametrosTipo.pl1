# programa con funcion
programa seis:

	variables 
		c, d,suma: entero;
		e : booleano;
	subprogramas
		funcion sumar (a,b : entero): entero:
			variables suma: entero;
		comienzo
			suma = a + b;

			devolver suma;
		fin;
       

comienzo
	c = 5;
	d = 18;
	suma = sumar(c,e);

	escribir(suma);

fin.
